
# Build

```
npm install
npx playwright install
```

# Run

```
npx playwright test
```

# Build docker

```
docker build . -t test-kafka
```

# Run in docker

```
docker run --rm -it test-kafka
```
